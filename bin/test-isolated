#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

node -v
node "$basedir/../scripts/mocha" -t 10000 -R xunit "$basedir/../isolated.run"
